- name: loops using ansible
  hosts:  local
  connection: local
  tasks:
  - name: install pacakages
    ansible.builtin.debug:
      name: "{{ item }}"
      state: present
    loop:
    - mysql
    - nginx
    - zip
